<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Get Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora</h1>
    <% for(post of posts){ %>
        <div class="post" method="post" action="/post">
            <h3 class="user"><%= post.username %></h3>
            <h3 class="content"><%= post.content %></h3>
            <a href="http://localhost:8080/posts/<%= post.id %>">View in detail</a>
            <button><a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a></button>
            <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
                <button>Delete post</button>
            </form>
            

        </div>     
    <% } %>
    <br><br>
    <a href="http://localhost:8080/posts/new">Create New Post</a>
</body>
</html>